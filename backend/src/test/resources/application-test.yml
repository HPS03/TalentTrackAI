spring:
  datasource:
    url: jdbc:h2:mem:talenttrack;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
app:
  storage:
    upload-dir: ./target/test-uploads
  ai:
    base-url: http://localhost:65530
    timeout-seconds: 1
  seed:
    demo-data: false
