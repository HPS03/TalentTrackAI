# Copy to .env and change for anything beyond local development.
DB_USERNAME=talenttrack
DB_PASSWORD=talenttrack
MYSQL_ROOT_PASSWORD=rootpassword
# Generate with:  openssl rand -base64 48
JWT_SECRET=local-docker-secret-change-me-0123456789abcdef
ADMIN_EMAIL=admin@talenttrack.ai
ADMIN_PASSWORD=Admin@123
SEED_DEMO_DATA=true
# Port the website is published on (use 80 on a server)
WEB_PORT=3000
